import { OppFilterPipe } from './opp-filter.pipe';

describe('OppFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new OppFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
